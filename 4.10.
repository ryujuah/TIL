*Pandas 정의, 특징
-서로 다른 유형의 데이터를 공통의 형식으로 정리
-고수준의 연산 수행 가능
-데이터의 형태와 속성이 다양

*Pandas 설치
-pandas 임포트 및 및 버전 확인

*Pandas 객체
-1차원 배열 형태

*DataFrame
-여러 개의 시리즈가 모여 구성된 구조가 데이터프레임
-통합 인덱싱을 활용하여 데이터 조작 가능
-2차원 행렬이므로 행과 열을 나타내는 2개의 주소를 사용
-데이터프레임의 열은 공통의 속성을 나타내며, 
 행은 개별 데이터의 모임인 레코드를 나타냄

*Series
-리스트를 인자로 사용하여 시리즈 객체 생성
-values와 index 속성을 갖고 있음
-Series 변수의 values 값을 확인하면, 값에 해당하는 데이터가 array 형태로 추출이 되며,
시리즈 변수의 index 값을 확인하면, index 범위 값만을 얻어낼 수 있음

*Index
-인덱스로 문자열 사용 가능
-문자열 인덱스로 항목에 접근 가능
-인접하지 않거나 연속적이지 않은 인덱스를 사용하여 임의로 만들 수 있음

*딕셔너리 구조
-일반적으로 딕셔너리는 값에 키를 매핑하는 구조
-일련의 임의의 값에 임의의 키를 매핑하는 구조
-타입이 지정된 키를 일련의 타입이 지정된 값으로 매핑하는 구조

*데이터 프레임
-Series 객체의 연속
-population과 area(속성을 나타내는 값)로부터 데이터프레임이 구성되었음을 확인할 수 있음
-인덱스 라벨을 이용하여 요솟값에 접근할 수 있는 index 속성을 갖고 있음
- 2차원 배열로 볼 수도 있기 때문에, 열 이름에 대한 라벨을 나타내는 index 객체인 columns 속성도 가지고 있음
